services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: cartei
      POSTGRES_PASSWORD: cartei
      POSTGRES_DB: cartei
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
